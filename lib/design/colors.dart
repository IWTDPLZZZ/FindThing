import 'package:flutter/material.dart';

const Color blueName = Color(0xFF2600FF);
const Color lightBlue = Color(0xFF0091FF);
/// Deeper blue for active controls (chips, FAB) — pairs with [lightBlue] accent.
const Color accentBlueDeep = Color(0xFF0066CC);
const Color gray = Color(0xFF696969);
const Color blackName = Color(0xFF1A1C1E);
const Color grayField = Color(0xFFF0F2F5);
const Color grayFieldText = Color(0xFFB3B1B1);
const Color memory = Color(0xFF3705FF);
/// App canvas behind cards and lists (Material 3 style neutral).
const Color surfaceCanvas = Color(0xFFF8F9FA);
/// Subtle borders for inputs and outlined controls.
const Color outlineSubtle = Color(0xFFE0E3E8);